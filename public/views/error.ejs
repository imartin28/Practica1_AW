<!DOCTYPE html>
    <html>
        <head>
            <title>Error del servidor</title>
            <meta charset="UTF-8">
        </head>

        <body>
            <h1><%= nombreError %></h1>
            <p>Se ha producido el siguiente error: <%= mensaje %></p>
            <% if (pila != null){ %>
                <p>Pila de ejecución:</p>
                <pre><%= pila %></pre>
            <% } %>
        </body>
    </html>