<!DOCTYPE html>
	<html lang="es">
	<head>
		<title>FaceBluff</title>
        
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
        <link href="/css/style.css" rel="stylesheet" media="screen"/>
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">

	</head>

	<body>
        <div class="contenedor">
            <div class="header">
                <div class="logo">
                
                </div>
            </div>
            
            <div class="panelInicioSesion">
                <h1>Iniciar sesión</h1>
                            
                <form method="POST" action="login" class="formulario-login">

                    <input class="input-inicio-sesion" type="text" name="email" placeholder="Email"/>
                   
                    <% if (errores != null && errores.email) { %>
                        <span class="red"><%= errores.email.msg %></span>
                    <% } %> 

                    <input class="input-inicio-sesion" type="password" name="password" placeholder="Contraseña"/>
                    <% if (errores != null && errores.password) { %>
                        <span class="red"><%= errores.password.msg %></span>
                    <% } %>  

                    <button type="submit" class="botonAcceder">Acceder</button>
                </form>

              

                
                <div class="registro">
                    <span>¿No tienes una cuenta?</span>
                    <a href="/users/new_user">Crear nueva cuenta</a>
                </div>

                
            </div>
        </div>    
	</body>
</html>